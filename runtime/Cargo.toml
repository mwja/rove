[package]
name = "runtime"
version = "0.1.0"
edition = "2024"

[dependencies]

[lib]
crate-type = ["staticlib"]

# Cargo.toml for the runtime
[profile.release]
panic = "abort"
lto = true
codegen-units = 1
strip = true
